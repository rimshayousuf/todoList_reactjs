map method: We use map when we have to create a new array.

foreach method: We use foreach when we have to perform any operation on the
existence element of an array.

filter method: Filters an array with values that passes a test.
what do you do?
you give a function, the values for which the function is
returning true, the you will see these values in your new array. 